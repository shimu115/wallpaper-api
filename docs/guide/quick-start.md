# 说明
**ip以本机 `localhost` 为例**
* 查看结果需要自行 [部署项目](../deploy/deploy.md) ，否则会无法访问接口
* ~~此结构直接会返回一张图片，若部署服务器，请注意会消耗服务器流量~~
* `bing今日图片` `bing随机图片` `acg随机图片` 三个接口均添加了 `askMethod` 请求方式具体参考接口说明
    **若部署云服务器需注意 `askMethod` 请求方式为 `stream` 则依旧会消耗服务器流量，接口不传 `askMethod` 请求方式则默认使用 `stream` 响应方式**
* 此处所有接口仅给出一个简单示例，详细参数请直接查看 [接口文档](../api/api.md)
* 所有接口均为 `GET` 请求
